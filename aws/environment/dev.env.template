# Development Environment Configuration Template for AWS Deployment
# Copy this file to dev.env and fill in the actual values
# This template is designed to work with GitHub Secrets

# ========================================
# AWS Configuration
# ========================================
AWS_ACCOUNT_ID=${AWS_ACCOUNT_ID}
AWS_REGION=${AWS_REGION:-us-east-1}
ENVIRONMENT=${ENVIRONMENT:-dev}

# ========================================
# Auth0 Configuration
# ========================================
AUTH0_ISSUER_URI=${AUTH0_ISSUER_URI}
AUTH0_AUDIENCE=${AUTH0_AUDIENCE}

# ========================================
# CORS Configuration
# ========================================
CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS:-http://localhost:5173,http://localhost:3000}

# ========================================
# Database Configuration
# ========================================
# RDS PostgreSQL (Orders & Auth)
DB_USERNAME=${DB_USERNAME:-shoplite}
DB_PASSWORD=${DB_PASSWORD}

# DocumentDB (Catalog)
DOCUMENTDB_USERNAME=${DOCUMENTDB_USERNAME:-shoplite}
DOCUMENTDB_PASSWORD=${DOCUMENTDB_PASSWORD}

# ========================================
# ECR Repository URLs
# ========================================
ECR_REGISTRY=${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com
ECR_EUREKA_IMAGE=${ECR_REGISTRY}/shoplite-eureka:latest
ECR_API_GATEWAY_IMAGE=${ECR_REGISTRY}/shoplite-api-gateway:latest
ECR_ORDER_IMAGE=${ECR_REGISTRY}/shoplite-order:latest
ECR_CATALOG_IMAGE=${ECR_REGISTRY}/shoplite-catalog:latest
ECR_AUTH_IMAGE=${ECR_REGISTRY}/shoplite-auth:latest
ECR_FRONTEND_IMAGE=${ECR_REGISTRY}/shoplite-frontend:latest

# ========================================
# Service Discovery
# ========================================
SERVICE_DISCOVERY_NAMESPACE=${ENVIRONMENT}-shoplite.local

# ========================================
# Jaeger Configuration
# ========================================
JAEGER_ENDPOINT=http://jaeger.${ENVIRONMENT}-shoplite.local:9411/api/v2/spans

# ========================================
# Load Balancer Configuration
# ========================================
ALB_DNS_NAME=${ALB_DNS_NAME}
ALB_URL=http://${ALB_DNS_NAME}

# ========================================
# Application URLs
# ========================================
FRONTEND_URL=${ALB_URL}
API_GATEWAY_URL=${ALB_URL}/api
JAEGER_UI_URL=${ALB_URL}:16686

# ========================================
# Monitoring and Logging
# ========================================
CLOUDWATCH_LOG_GROUP=/ecs/${ENVIRONMENT}-shoplite
CLOUDWATCH_LOG_REGION=${AWS_REGION}

# ========================================
# Cost Optimization
# ========================================
# Use FARGATE_SPOT for cost savings in dev
ECS_CAPACITY_PROVIDER=${ECS_CAPACITY_PROVIDER:-FARGATE_SPOT}
ECS_CPU=${ECS_CPU:-256}
ECS_MEMORY=${ECS_MEMORY:-512}

# ========================================
# Security
# ========================================
# Enable encryption at rest and in transit
ENABLE_ENCRYPTION=${ENABLE_ENCRYPTION:-true}
ENABLE_SSL=${ENABLE_SSL:-false}  # Set to true for production

# ========================================
# Backup and Retention
# ========================================
BACKUP_RETENTION_DAYS=${BACKUP_RETENTION_DAYS:-1}
LOG_RETENTION_DAYS=${LOG_RETENTION_DAYS:-7}
